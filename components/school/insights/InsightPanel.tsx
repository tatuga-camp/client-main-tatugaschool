// Moved to components/common/Panel.tsx so admin pages share the same card
// language. Kept as a re-export so existing Insights imports keep working.
export { Panel, EmptyState } from "../../common/Panel";
